<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Space Mono', monospace;
            margin: 0;
            padding: 0;
            background: linear-gradient(to bottom, #0a192f, #112240);
            color: #ccd6f6;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100dvh;
        }

        h1 {
            font-size: 2rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
            color: #64ffda;
            letter-spacing: 1px;
            font-weight: 600;
        }

        /* Form Styling */
        form {
            display: flex;
            gap: 0.75rem;
            align-items: center;
            background: rgba(17, 34, 64, 0.5);
            padding: 1rem;
            border-radius: 12px;
            backdrop-filter: blur(12px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        input {
            padding: 0.8rem;
            border: none;
            background: rgba(255, 255, 255, 0.1);
            color: #ccd6f6;
            border-radius: 10px;
            outline: none;
            width: 250px;
            transition: 0.3s;
            text-align: center;
            font-size: 1rem;
        }

        input:focus {
            background: rgba(255, 255, 255, 0.15);
            transform: scale(1.02);
        }

        button {
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, #64ffda, #48e5c2);
            color: #0a192f;
            border: none;
            cursor: pointer;
            border-radius: 12px;
            font-weight: bold;
            font-size: 1rem;
            transition: all 0.3s ease-in-out;
            box-shadow: 0 5px 15px rgba(100, 255, 218, 0.3);
            margin: rem;
        }

        button:hover {
            background: linear-gradient(135deg, #ff6b6b, #ff4040);
            box-shadow: 0 8px 20px rgba(255, 107, 107, 0.4);
            transform: scale(1.05);
        }

        /* Table Styling */
        table {
            text-align: center;
            border-collapse: collapse;
            margin-top: 2rem;
            width: fit-content;
            background: rgba(17, 34, 64, 0.6);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
        }

        th,
        td {
            padding: 1rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        th {
            background: rgba(17, 34, 64, 0.8);
            color: #64ffda;
            border-bottom: 1px solid white;
        }

        tr:nth-child(even) {
            background: rgba(255, 255, 255, 0.05);
        }

        tr:nth-child(odd) {
            background: rgba(255, 255, 255, 0.08);
        }

        /* Links */
        a {
            color: #64ffda;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s;
        }

        a:hover {
            color: #ff6b6b;
            text-decoration: underline;
        }

        /* Redirect URL Column */
        .redirect-url {
            text-align: left;
        }
    </style>
</head>

<body>
    <h1>URL Shortner</h1>
    <% if (locals.id) { %>
        <p>
            URL Generated: <a href="http://localhost:3001/url/<%= id %>" target="_blank">http://localhost:3001/url/<%=
                    id %></a>
        </p>
        <% } %>
            <div>
                <form action="/url" method="POST">
                    <label for="url">Enter your original URL:</label>
                    <input type="text" name="url" id="url" >
                    <button type="submit">Generate</button>
                </form>
            </div>
            <div>
                <% if (locals.urls) { %>
                    <table>
                        <thead>
                            <th>S. No</th>
                            <th>ShortID</th>
                            <th>Redirect</th>
                            <th>Clicks</th>
                            <th>Generated URLs</th>
                        </thead>
                        <tbody>
                            <% urls.forEach((url, index)=> { %>
                                <tr>
                                    <td>
                                        <%= index+1 %>
                                    </td>
                                    <td>
                                        <%= url.shortID %>
                                    </td>
                                    <td class="redirect-url">
                                        <%= url.redirectURL %>
                                    </td>
                                    <td>
                                        <%= url.visitHistory.length %>
                                    </td>
                                    <td>
                                        <a href="http://localhost:3001/url/<%= url.shortID %>" target="_blank">
                                            http://localhost:3001/url/<%= url.shortID %>
                                        </a>

                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                    <% } %>
            </div>
</body>

</html>